# Contributing

1. Run `npm install`.
2. Run `npm run ci`.
3. Keep support-registry changes paired with fixtures and hash evidence.
4. Do not include Cursor credentials, dependency source dumps, or local runtime paths in commits.
5. Use a focused branch and pull request. Release/publish changes require the version-bump and Trusted Publishing checks.
